# GUAU · Variables de entorno del cliente Expo.
# Copia este archivo a `.env` para ejecutar la app: `cp .env.example .env`.
#
# Estas dos claves son PÚBLICAS por diseño (clave publishable de Supabase):
# la seguridad de los datos la garantizan las políticas RLS del servidor.
# Los secretos reales (service_role, claves de IA) viven SOLO en el servidor
# (Edge Functions) y nunca en este repositorio ni en el cliente.

EXPO_PUBLIC_SUPABASE_URL=https://jnyowsltrjsqkidgcgol.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_hifHmishV-2OhuvrOfqa3A_k3lkP4jW
